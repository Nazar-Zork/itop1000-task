import styles from './Footer.module.css';

const Footer = () => (
    <footer className={styles.footer}>
      <p className={styles.footer__text}>Task for ITOP1000</p>
    </footer>
);


export default Footer;
